import { Interaction, Message, Note } from "../types";

export type InteractionsResponse = {
    interactions: Interaction[];
};

export type AnInteractionResponse = {
    interaction: Interaction;
};
